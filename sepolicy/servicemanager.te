allow servicemanager gpsd:dir search;
allow servicemanager gpsd:file { open read };
allow servicemanager gpsd:process getattr;

allow servicemanager hdmi_helper:dir search;
allow servicemanager hdmi_helper:file { open read };
